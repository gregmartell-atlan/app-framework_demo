{
  "id": "github_token",
  "name": "GitHub Personal Access Token",
  "config": {
    "properties": {
      "token": {
        "type": "string",
        "ui": {
          "widget": "password",
          "label": "Personal Access Token",
          "help": "Fine-grained or classic PAT with repo + read:wiki scopes",
          "grid": 12,
          "rules": [
            { "required": true, "message": "Token is required" }
          ]
        }
      }
    }
  }
}
